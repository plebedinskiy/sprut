

Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
	
	Если ЭтоНовый() Тогда
		ДатаСозданияВБазе = ТекущаяДата();
		Автор = ПараметрыСеанса.ТекущийПользователь;
	КонецЕсли;
	
	ДатаПоследнегоОбновления = ТекущаяДата();
	
КонецПроцедуры
